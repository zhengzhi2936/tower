<tr>
<td> <%= todo.id%></td>
<td> <%= link_to team_project_todo_path(team, project, todo) do %>
  <%= todo.title %>
    <% end %>
</td>
<td> <%= link_to("编辑", edit_team_project_todo_path(team, project, todo), class: "btn btn-sm btn-success")%>
<%= link_to("删除", team_project_todo_path(team, project, todo), method: :delete, class: "btn btn-sm btn-danger", data: {confirm: "确定吗"})%></td>
<td> <%= todo.created_at%></td>
</tr>
